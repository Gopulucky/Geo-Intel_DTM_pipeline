# backend/requirements.txt
# Windows-compatible: uses pre-built binary wheels (no source compilation needed)
# Install order matters — numpy first, then geo stack.

# ── Web Framework ──────────────────────────────────────────
fastapi==0.111.0
uvicorn==0.30.1
python-multipart==0.0.9

# ── Numpy FIRST (rasterio/geopandas need it at build time) ─
numpy>=1.26,<2.0

# ── Geospatial Stack (flexible versions → pip picks wheels) ─
# rasterio: do NOT pin exact version on Windows — let pip find a pre-built wheel
rasterio>=1.3.6
GDAL>=3.4.0       ; sys_platform != "win32"
geopandas>=0.14.0
shapely>=2.0.0
pyproj>=3.6.0

# ── ML / Science ───────────────────────────────────────────
scipy>=1.13.0
scikit-learn>=1.5.0

# ── Data / Viz ─────────────────────────────────────────────
matplotlib>=3.9.0
pandas>=2.2.0
tqdm>=4.66.0
joblib>=1.4.0
Pillow>=10.3.0
folium>=0.16.0

# ── LiDAR ──────────────────────────────────────────────────
laspy[lazrs]>=2.5.0

# ── WhiteboxTools ──────────────────────────────────────────
whitebox>=2.3.4

# ── Utilities ──────────────────────────────────────────────
aiofiles>=23.2.1
requests>=2.31.0
